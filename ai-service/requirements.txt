# ai-service — OCR via Azure AI Document Intelligence (prebuilt-read).
# No local model, no GPU, no rasterising: Document Intelligence takes the raw PDF
# or image bytes directly and returns faithful text with per-page structure.
fastapi==0.136.1
uvicorn==0.46.0
azure-ai-documentintelligence==1.0.0
azure-identity==1.25.3
filetype==1.2.0
python-multipart==0.0.27
python-dotenv==1.2.2
