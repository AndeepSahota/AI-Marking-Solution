# Secrets — machine-specific, never commit
local.env

# Local sizing-test artifacts — not part of the service.
samples/
*.csv
chandra-response.json

# RAG exemplar store — contains real student essay content and embeddings,
# never commit. .gitkeep still tracked so the directory itself persists.
data/*.sqlite
data/*.sqlite-shm
data/*.sqlite-wal
data/*.sqlite-journal
