services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: aimira
      POSTGRES_PASSWORD: aimira
      POSTGRES_DB: aimira
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: